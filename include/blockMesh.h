#include <vector>
#include<cmath>

//
// produces a 3-D bliockmesh
//
template<typename num, typename iter>
class blockMesh{
  private:
    iter nx, ny, nz;    //Number of nodes in each direction
    iter nxe, nye, nze; //Number of elements in each direction

  public:

}
